---
type: claim
tags:
  - javascript
---

In [[JavaScript]] [[value]] is distinct from our code; it represent [[data]] that we interact with using our code, and generally, we cannot delete them, create new ones, or alter their core properties.
They are always ready to be accessed with our code, and they disappear once we start referring to them.
Values are distinct from our code; they represent data that we interact with using our code, and generally, we cannot delete them, create new ones, or alter their core properties.
They are always ready to be accessed with our code, and they disappear once we start referring to them. 
values + code (list of instructions) = application.