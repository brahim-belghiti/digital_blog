---
import Head from "@components/Head.astro";
import Header from "@components/structure/Header.astro";
import Footer from "@components/structure/Footer.astro";
import { SITE } from "@consts";

type Props = {
  title: string;
  description: string;
};

const { title, description } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <style is:inline>html{visibility:hidden}html.dark{background:#1c1917;color-scheme:dark}html:not(.dark){background:#f5f5f4}</style>
    <script is:inline>
      const t = localStorage.getItem("theme");
      if (t === "dark" || (t !== "light" && window.matchMedia("(prefers-color-scheme: dark)").matches)) {
        document.documentElement.classList.add("dark");
      }
    </script>
    <Head title={`${title} | ${SITE.NAME}`} description={description} />
  </head>
  <body>
    <Header />
    <main>
      <slot />
    </main>
    <Footer />
  </body>
</html>
